name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "building"
  deploy:
    runs-on: ubuntu-latest
    needs: [nonexistent-job]
    steps:
      - run: echo "deploying"
